<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Placement Drives</title>
  <link href="https://unpkg.com/@picocss/pico@2/css/pico.min.css" rel="stylesheet" />
  <style>
    :root {
      --primary-color: #2563eb;
      --danger-color: #ef4444;
      --primary-hover: #1d4ed8;
      --danger-hover: #dc2626;
      --bg-light: #f8fafc;
    }

    body {
      background-color: var(--bg-light);
      color: #1f2937;
      font-family: "Inter", system-ui, sans-serif;
    }

    main.container {
      padding-top: 2rem;
      padding-bottom: 3rem;
      max-width: 1100px;
    }

    header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 2rem;
      flex-wrap: wrap;
      gap: 1rem;
    }

    header h1 {
      margin: 0;
      font-size: 2rem;
      font-weight: 600;
      color: #0f172a;
    }

    a[role="button"] {
      font-size: 1rem;
      padding: 0.75rem 1.4rem;
      border-radius: 0.6rem;
      background: var(--primary-color);
      color: white;
      border: none;
      transition: background 0.2s ease;
      text-decoration: none;
    }

    a[role="button"]:hover {
      background: var(--primary-hover);
    }

    table {
      width: 100%;
      border-collapse: separate;
      border-spacing: 0 0.7rem;
    }

    th {
      background: #f1f5f9;
      padding: 1rem;
      font-weight: 600;
      text-align: left;
      border-bottom: 2px solid #e2e8f0;
      font-size: 0.95rem;
      color: #334155;
    }

    td {
      background: #fff;
      padding: 1rem;
      vertical-align: middle;
      border-top: 1px solid #e5e7eb;
      font-size: 0.95rem;
    }

    tbody tr {
      border-radius: 0.75rem;
      transition: all 0.2s ease;
    }

    tbody tr:hover {
      transform: translateY(-2px);
      box-shadow: 0 3px 10px rgba(0, 0, 0, 0.08);
    }

    .status {
      padding: 0.3rem 0.75rem;
      border-radius: 999px;
      font-size: 0.8rem;
      font-weight: 600;
      display: inline-block;
      text-align: center;
    }

    .active {
      background: #dcfce7;
      color: #166534;
    }

    .completed {
      background: #f3f4f6;
      color: #6b7280;
    }

    /* Button Group */
    .actions {
      display: flex;
      justify-content: center;
      align-items: center;
      gap: 0.6rem;
    }

    .btn {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      width: 90px;
      height: 38px;
      font-size: 0.9rem;
      border: none;
      border-radius: 0.4rem;
      color: #fff;
      font-weight: 500;
      cursor: pointer;
      transition: all 0.2s ease;
      text-decoration: none;
    }

    .btn.edit {
      background: var(--primary-color);
    }

    .btn.edit:hover {
      background: var(--primary-hover);
      transform: translateY(-1px);
    }

    .btn.delete {
      background: var(--danger-color);
    }

    .btn.delete:hover {
      background: var(--danger-hover);
      transform: translateY(-1px);
    }

    .empty-state {
      text-align: center;
      background: white;
      border: 2px dashed #d1d5db;
      border-radius: 0.75rem;
      padding: 3rem 1rem;
      margin-top: 3rem;
      color: #6b7280;
    }

    .empty-state a {
      background: var(--primary-color);
      color: white;
      padding: 0.8rem 1.2rem;
      border-radius: 0.6rem;
      text-decoration: none;
      display: inline-block;
      margin-top: 1rem;
      transition: background 0.2s ease;
    }

    .empty-state a:hover {
      background: var(--primary-hover);
    }

    footer {
      text-align: center;
      color: #94a3b8;
      margin-top: 2rem;
      font-size: 0.9rem;
    }
  </style>
</head>

<body>
  <main class="container">
    <header>
      <h1>Placement Drives</h1>
      <a href="/tpo/drives/new" role="button">+ Add New Drive</a>
    </header>

    <% if (drives.length === 0) { %>
      <div class="empty-state">
        <h3>No Placement Drives Yet</h3>
        <p>Start by adding your first company drive.</p>
        <a href="/tpo/drives/new">Create Drive</a>
      </div>
    <% } else { %>
      <table>
        <thead>
          <tr>
            <th>Company</th>
            <th>Title</th>
            <th>Mode</th>
            <th>Venue</th>
            <th>Status</th>
            <th>Last Updated</th>
            <th style="text-align:center;">Actions</th>
            <th style="text-align:center;">Round results</th>
          </tr>
        </thead>
        <tbody>
          <% drives.forEach(d => { %>
            <tr>
             <td>
  <a href="/tpo/drives/<%= d._id %>" style="color:#2563eb; text-decoration:none; font-weight:600;">
    <%= d.company %>
  </a>
</td>
              <td><%= d.title %></td>
              <td><%= d.mode %></td>
              <td><%= d.venue || '—' %></td>
              <td>
                <span class="status <%= d.isActive ? 'active' : 'completed' %>">
                  <%= d.isActive ? 'Active' : 'Completed' %>
                </span>
              </td>
              <td><%= new Date(d.updatedAt).toLocaleString() %></td>
              <td>
                <div class="actions">
                  <a href="/tpo/drives/<%= d._id %>/edit" class="btn edit">Edit</a>
                  <form method="post" action="/tpo/drives/<%= d._id %>/delete">
                    <button type="submit" class="btn delete">Delete</button>
                  </form>
                </div>
              </td>
              <td><a href="/tpo/round-results/show/<%= d._id %>">Round Results</a></td>
            </tr>
          <% }) %>
        </tbody>
      </table>
    <% } %>

    <footer>
      © <%= new Date().getFullYear() %> Placement Portal | TPO Dashboard
    </footer>
  </main>
</body>
</html>
