<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Student Dashboard</title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>

<div class="center">
  <div class="card">
    <h1>Welcome, <%= user.name %> 👋</h1>

    <!-- Profile Status -->
    <% if (user.profileComplete) { %>
      <p style="color: green;">✅ Profile Completed</p>
    <% } else { %>
      <p style="color: red;">⚠ Profile Incomplete — <a href="/student/profile">Update Now</a></p>
    <% } %>

    <!-- Stats -->
    <h3>📊 Placement Overview</h3>
    <p>Available Drives: <b><%= user.totalDrives %></b></p>
    <p>Applications Submitted: <b><%= user.appliedDrives %></b></p>

    <!-- Buttons / Links -->
    <a href="/student/profile"><button class="btn">Edit Profile</button></a>
    <a href="/student/drives"><button class="btn">View & Apply for Drives</button></a>
    <a href="/student/applications"><button class="btn accent">View Application Status</button></a>
  </div>
</div>

</body>
</html>
